<h1>商品一覧</h1>

<% @items.each do |item| %>
  <p>商品名:<%= item.name %></p>
  <p>価格:<%= item.price %></p>
  <p>説明:<%= item.description %></p>

  <%= link_to '購入する', items_buy_path(item) %>
  <p>================</p>
<% end %>